codecov:
  max_report_age: off
comment: false
coverage:
  precision: 2
  round: down
  status:
    patch:
      default:
        enabled: no
        if_not_found: success
    changes:
      default:
        enabled: no
        if_not_found: success